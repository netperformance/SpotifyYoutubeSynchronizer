# Praekompilierte CPU-Wheels fuer llama-cpp-python (kein C++-Compiler noetig).
# Betrifft nur das Paket unten - alle anderen Pakete kommen weiterhin von PyPI.
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu

fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
python-dotenv==1.0.1
itsdangerous==2.2.0
tzdata==2025.2

# Optionale lokale KI-Unterstuetzung fuer unsichere Treffer (siehe app/llm.py).
# Das Modell selbst (~1.3 GB) wird NICHT hier installiert, sondern beim ersten
# Aktivieren der "KI-Unterstuetzung"-Checkbox in der App automatisch nachgeladen
# (Download-Fortschritt direkt in der UI sichtbar).
llama-cpp-python==0.3.34
